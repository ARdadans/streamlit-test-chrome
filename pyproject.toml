[project]
name = "streamlit-test-chrome"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ahmad Ramadhan", email = "ahrdadan@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "patchright>=1.62.3",
    "streamlit>=1.63.0",
]

[project.scripts]
streamlit-test-chrome = "streamlit_test_chrome:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
